#ifndef MODEL_H
#define MODEL_H


/**
 * @class model
 * @brief Loads and configures a TensorFlow Lite model for Edge TPU inference.
 *
 * The model class handles the loading of a TFLite model from disk and prepares it
 * for inference on the Edge TPU. It also allows the configuration of the model
 * with the Edge TPU delegate, which enables hardware acceleration. This class
 * provides access to input and output tensors for easy data management.
 */
class model
{
 // Load model
 // Set Delegate
 // Get Input Tensor
 // Get Output Tensor
};



#endif //MODEL_H
