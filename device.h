#ifndef DEVICE_H
#define DEVICE_H


/**
 * @class device
 * @brief Manages the initialization, configuration, and state of the Edge TPU or MCU device.
 *
 * The device class is responsible for setting up the hardware context for the Edge TPU
 * or microcontroller, ensuring it is correctly initialized and ready for inference. It
 * provides methods to check the device status, reset if needed, and retrieve device-specific
 * information, which can be useful for performance optimization.
 */
class device
{
 // Initialize
 // Shutdown
 // Check and return device status
};



#endif //DEVICE_H
